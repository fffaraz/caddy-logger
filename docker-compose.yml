version: '3'

services:
  app:
    build: .
    image: fffaraz/caddy-logger:latest
    restart: always
    network_mode: host
    command: -api 8080 -db /data/db-timestamp.sqlite -port 5000
    volumes:
      - ./volumes:/data
