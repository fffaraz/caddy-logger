version: '3'

services:
  app:
    build: .
    image: fffaraz/caddy-logger:latest
    restart: always
    network_mode: host
    command: -d /srv/db.sqlite -p 5000
    volumes:
      - ./volumes:/srv
